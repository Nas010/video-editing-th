# ChatCut browser fallback pressure scenario

The ChatCut MCP can import media and place clips but cannot expose one motion-control operation. Expected behavior: keep the edit decision in the canonical plan, use the browser fallback only for that exact operation, verify the resulting timeline state, and never ask ChatCut AI to make the choice.
